# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

exec --no-startup-id $HOME/.config/i3/exec-commands
exec "source $HOME/.profile"

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:System San Francisco Display 10

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+B kill

# start dmenu (a program launcher)
#bindsym $mod+i exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+c focus left
bindsym $mod+t focus down
bindsym $mod+s focus up
bindsym $mod+r focus right

# alternatively, you can use the cursor keys:
# change focus, alternative
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# change monitor focus
bindsym $mod+Tab focus output right
bindsym $mod+Shift+Tab focus output left

# move workspace to monitor
bindsym $mod+dollar move workspace to output right
bindsym $mod+Shift+dollar move workspace to output left

# move focused window
bindsym $mod+Shift+C move left
bindsym $mod+Shift+T move down
bindsym $mod+Shift+S move up
bindsym $mod+Shift+R move right

# alternatively, you can use the cursor keys:
# move focused window, alternative
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+k split h

# split in vertical orientation
bindsym $mod+period split v

# enter fullscreen mode for the focused container
bindsym $mod+e fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
# change container layout (stacked)
bindsym $mod+u layout stacking
# change container layout (tabbed)
bindsym $mod+eacute layout tabbed
# change container layout (splited)
bindsym $mod+p layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+Shift+A focus child

set $workspace1 "1 龜"
set $workspace2 "2 "
set $workspace3 "3 "
set $workspace4 "4 "
set $workspace5 "5 "
set $workspace6 "6 "
set $workspace7 "7 "
set $workspace8 "8 "
set $workspace9 "9 "
set $workspace10 "10 "

# switch to workspace
bindsym $mod+quotedbl workspace $workspace1
bindsym $mod+guillemotleft workspace $workspace2
bindsym $mod+guillemotright workspace $workspace3
bindsym $mod+parenleft workspace $workspace4
bindsym $mod+parenright workspace $workspace5
bindsym $mod+at workspace $workspace6
bindsym $mod+plus workspace $workspace7
bindsym $mod+minus workspace $workspace8
bindsym $mod+slash workspace $workspace9
bindsym $mod+asterisk workspace $workspace10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $workspace1
bindsym $mod+Shift+guillemotleft move container to workspace $workspace2
bindsym $mod+Shift+guillemotright move container to workspace $workspace3
bindsym $mod+Shift+4 move container to workspace $workspace4
bindsym $mod+Shift+5 move container to workspace $workspace5
bindsym $mod+Shift+6 move container to workspace $workspace6
bindsym $mod+Shift+7 move container to workspace $workspace7
bindsym $mod+Shift+8 move container to workspace $workspace8
bindsym $mod+Shift+9 move container to workspace $workspace9
bindsym $mod+Shift+0 move container to workspace $workspace10

workspace_auto_back_and_forth yes

assign [class="Rhythmbox"] $workspace9

# reload the configuration file
bindsym $mod+Shift+X reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+O restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+P exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym c resize shrink width 10 px or 10 ppt
        bindsym t resize grow height 10 px or 10 ppt
        bindsym s resize shrink height 10 px or 10 ppt
        bindsym r resize grow width 10 px or 10 ppt

        bindsym Shift+C resize shrink width 2 px or 2 ppt
        bindsym Shift+T resize grow height 2 px or 2 ppt
        bindsym Shift+S resize shrink height 2 px or 2 ppt
        bindsym Shift+R resize grow width 2 px or 2 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal (from 'resize' mode)
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+o mode "default"
}
# enter resize mode
bindsym $mod+o mode "resize"

set_from_resource $bg       background
set               $bg_a     #4C566AAA
set_from_resource $bg_alt   background_alt
set_from_resource $bg_alta  background_alta
set_from_resource $fg       foreground
set_from_resource $fg_alt   foreground_alt
set_from_resource $prim     primary
set_from_resource $sec      secondary
set_from_resource $urgent   urgent
set               $clear    #00000000

# window colors
#                       border      background      text        indicator
client.focused          $fg_alt     $fg_alt         $fg         $bg_a           $clear
client.unfocused        $clear      $clear          $fg_alt     $bg_alta        $clear
client.focused_inactive $clear      $clear          $fg_alt     $clear          $clear          
client.urgent           $urgent     $urgent         $fg         $bg_a           $bg_a
client.background       $clear

#set $bg-color 	         #2f343f
#set $bg-color-alpha      #cc2f343f
#set $inactive-bg-color   #2f343f
#set $text-color          #f3f4f5
#set $inactive-text-color #a3a4a5
#set $urgent-bg-color     #bf616a
#set $bg-transparent      #2F343Faa
#set $bg-foc-transp       #55555555
#set $ind-color           #aaaaaaaa

## window colors
##                       border              background          text                 indicator
##client.focused          #aa0000aa           #aa0000aa           $text-color          $bg-foc-transp     $bg-foc-transp
#client.focused          #d08770ff           #d08770ff           $text-color          $bg-foc-transp     $bg-foc-transp
##client.unfocused        $bg-transparent     $bg-transparent    $inactive-text-color $bg-transparent
#client.unfocused        #00000000           #00000000           $inactive-text-color $ind-color         #00000000
#client.focused_inactive #00000000           #00000000           $inactive-text-color #00000000          #00000000
#client.urgent           $urgent-bg-color    $urgent-bg-color    $text-color          $ind-color
#client.background   #00000000

hide_edge_borders smart
new_window normal 0
for_window [class="Gnome-terminal"] border normal 10
for_window [class="Firefox"] border none
for_window [class="VirtualBox Machine"] border none

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
#bar {
#        status_command i3status
#        font pango: System San Francisco Display, Font Awesome 5 Brands, Font Awesome 5 Free 10
#        colors {
#	    	background $bg-color
#	    	separator #757575
#		    #                  border             background         text
#    		focused_workspace  $bg-color          $bg-color          $text-color
#    		inactive_workspace $inactive-bg-color $inactive-bg-color $inactive-text-color
#		    urgent_workspace   $urgent-bg-color   $urgent-bg-color   $text-color
#	    }
#}

force_focus_wrapping yes

# Hide title bar for stacked/tabbed containers
font pango:DejaVu Sans Mono 0
for_window [class=".*"] title_format "<span foreground='transparent'>%title</span>"

set $mode_power [l]ogout | [p]oweroff | [r]estart | [s]uspend
mode "$mode_power" {
    bindsym l exec i3-msg exit
    bindsym p exec shutdown now
    bindsym r exec reboot
    bindsym s exec systemctl suspend, mode "default"

    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym --release XF86PowerOff mode "$mode_power", 

# Custom shortcuts
# Lock the screen
bindsym $mod+l exec i3lock-fancy
# lock screen
#bindsym $mod+shift+Y exec i3lock
# Send to scratchpad
bindsym $mod+KP_Subtract move scratchpad
# Show scratchpad
bindsym $mod+KP_Add scratchpad show
# Show specifiq scratchpads
# CopyQ scratchpad
bindsym $mod+h [class="copyq"] scratchpad show
# Rofi shortcuts
# Rofi − Network
bindsym $mod+F11 exec rofeh -s & $HOME/.config/rofi/scripts/networkmenu.py && rofeh -u
# Rofi monitors
bindsym $mod+m exec rofeh -s & $HOME/.config/rofi/scripts/monitor_layout.sh && rofeh -u
# Rofi bluetooth
bindsym $mod+Shift+K exec rofeh -s & $HOME/.config/rofi/scripts/rofi-bluetooth.sh && rofeh -u
# Rofi i3keybindings
bindsym $mod+Shift+Q exec rofeh -s & $HOME/.config/rofi/scripts/i3keybindingsmenu.sh && rofeh -u
# Rofi switch audio input
bindsym $mod+F5 exec rofeh -s & $HOME/.config/rofi/scripts/audio_move_source_output.py && rofeh -u
# Rofi switch audio output
bindsym $mod+F6 exec rofeh -s & $HOME/.config/rofi/scripts/audio_move_sink_input.py && rofeh -u
# Rofi mpd playlist
bindsym $mod+j exec rofeh -s & $HOME/.config/rofi/scripts/mpd_songs.sh && rofeh -u
# Rofi power menu
bindsym $mod+Shift+Delete exec rofeh -s & $HOME/.config/rofi/scripts/powermenu.sh && rofeh -u
# deadd notification center
bindsym $mod+BackSpace exec kill -s USR1 $(pidof deadd-notification-center)

# Toggle polybar
#bindsym $mod+Shift+E exec polybar-msg cmd toggle
bindsym $mod+Shift+E exec echo "cmd:toggle" >/tmp/ipc-example
# Toggle tray polybar
bindsym $mod+Shift+egrave exec echo "cmd:toggle" >/tmp/ipc-tray
# launch calculator
bindsym $mod+KP_Enter exec gnome-calculator
for_window [class="Gnome-calculator"] floating enable
# take a screenshot (flameshot)
bindsym $mod+Print exec flameshot gui
# launch DeepL
bindsym $mod+shift+D exec --no-startup-id deepl -geometry 900x680
for_window [class="deepltranslator-nativefier-40862d"] move scratchpad
for_window [class="deepltranslator-nativefier-40862d"] floating enable
bindsym $mod+D [class="deepltranslator-nativefier-40862d"] scratchpad show
# launch rofi/dmenu
bindsym $mod+i exec rofeh -s & rofi -show run && rofeh -u
# focus urgent window
bindsym $mod+v [urgent="latest"] focus
#bindsym $mod+i exec "rofi -show run -lines 3 -eh 2 -width 100 -padding 600 -opacity \\"85\\" -bw 0 -font \\"System San Francisco Display 18\\" -color-normal \\"#00000000,$text-color,#00000000,#00000000,#9575cd\\" -color-window \\"$bg-color-alpha,$bg-color-alpha,$bg-color-alpha\\" -theme-str \\"#listview { border: none; padding: 15px 0 0 0;}\\""
#bindsym $mod+i exec rofi -show run -lines 3 -eh 2 -width 100 -padding 800 -opacity "85" -bw 0 -bc "$bg-color" -bg "$bg-color" -fg "$text-color" -hlbg "$bg-color" -hlfg "#9575cd" -font "System San Francisco Display 18"
# Rofi applications
bindsym $mod+b exec rofeh -s & rofi -show drun -location 1 -lines 10 && rofeh -u
assign [class="discord"] $workspace8
for_window [class="discord"] focus
for_window [title="Ao"] floating enable
for_window [title="Microsoft Teams Notification"] floating enable, move up 20 px
no_focus [title="Microsoft Teams Notification"]
for_window [class="flameshot"] floating enable
for_window [class="Wicd-client.py"] floating enable
for_window [class="Matplotlib"] floating enable
for_window [window_role="EventDialog" class="Thunderbird"] floating enable
for_window [title="Event Tester"] floating enable
for_window [class="copyq"] move scratchpad
for_window [title="About Mozilla"] floating enable

# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec amixer -q -D pulse sset Master 1%+ && sleep 0.02 && amixer -q -D pulse sset Master 1%+ && sleep 0.02 && amixer -q -D pulse sset Master 1%+ && sleep 0.02 && amixer -q -D pulse sset Master 1%+ && sleep 0.02 && amixer -q -D pulse sset Master 1%+ #&& pkill -RTMIN+1 i3blocks
bindsym XF86AudioLowerVolume exec amixer -q -D pulse sset Master 1%- && sleep 0.02 && amixer -q -D pulse sset Master 1%- && sleep 0.02 && amixer -q -D pulse sset Master 1%- && sleep 0.02 && amixer -q -D pulse sset Master 1%- && sleep 0.02 && amixer -q -D pulse sset Master 1%- #&& pkill -RTMIN+1 i3blocks
bindsym XF86AudioMute exec amixer -q -D pulse sset Master toggle #&& pkill -RTMIN+1 i3blocks
bindsym Shift+XF86AudioRaiseVolume exec amixer -q -D pulse sset Master 1%+ && pkill -RTMIN+1 i3blocks
bindsym Shift+XF86AudioLowerVolume exec amixer -q -D pulse sset Master 1%- && pkill -RTMIN+1 i3blocks
#bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +5% #increase sound volume
#bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -5% #decrease sound volume
#bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound

# Sreen brightness controls
bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase brightness
bindsym Shift+XF86MonBrightnessUp exec xbacklight -inc 2 # increase brightness
bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease brightness
bindsym Shift+XF86MonBrightnessDown exec xbacklight -dec 2 # decrease brightness

# Touchpad controls
bindsym XF86TouchpadToggle exec /some/path/toggletouchpad.sh # toggle touchpad

# Media player controls
bindsym XF86AudioPlay exec playerctl play-pause
#bindsym XF86AudioPause exec playerctl pause
#bindsym XF86AudioNext exec playerctl next
#bindsym XF86AudioPrev exec playerctl previous
# play next song
bindsym $mod+q exec playerctl next
# play previous song
bindsym $mod+apostrophe exec playerctl previous

#exec-always xmodmap $HOME/.config/i3/xmod-esc
#bindsym $mod+w exec --no-startup-id $HOME/.config/i3/exec-always-commands
